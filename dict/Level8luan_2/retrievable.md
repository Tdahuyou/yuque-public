# 音标

- 英 `/undefined/`
- 美 `/undefined/`

# 词义

adj. 可取回的；可补偿的；可检索的


# 同义

adj. 可取回的；可补偿的；可检索的
`compensable` `reimbursable`

# 同根

- n. `retrieve` [计] 检索；恢复，取回
- n. `retrieval` 检索；恢复；取回；拯救
- n. `retriever` （训练成能寻回猎物的）猎犬；取回的人
- vi. `retrieve` 找回猎物
- vt. `retrieve` [计] 检索；恢复；重新得到

